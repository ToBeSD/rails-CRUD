<h1>Rails 投稿一覧</h1>

<% if notice %>
  <p class="notice"><%= notice %></p>
<% end %>

<ul>
  <% @posts.each do |post| %>
    <li><%= post.title %></li>
  <% end %>
</ul>

<a href="<%= new_post_path %>">新しい投稿を作成する</a>
